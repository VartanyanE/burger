<li>
    {{burger_name}}

    <button class="change-status btn btn-primary" data-id="{{id}}" data-newstatus="{{devoured}}">
        {{#if devoured}}DEVOUR IT!{{else}}{{/if}}
    </button>

    <button class="delete-burger btn btn-danger" data-id="{{this.id}}">DELETE!</button>
</li>